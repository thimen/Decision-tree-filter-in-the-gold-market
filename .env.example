# Copy this file to `.env` and fill in your broker trade account credentials.
# The code reads these from the shell environment; it does not auto-load `.env`.

export MT5_LOGIN=12345678
export MT5_PASSWORD='your_trade_account_password'
export MT5_SERVER='YourBroker-Server'
